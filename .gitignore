/target
Cargo.lock.orig
.DS_Store
.local/

# web/ arrives at M1
node_modules/
dist/
*.wasm.d.ts
crates/cuttl-wasm/pkg/
web/pkg/
# Built release assets are attached to GitHub Releases, not committed to source.
/releases
# Dev TLS, made by `npm run cert`. Local to one machine and it holds a private
# key — regenerating it is one command.
web/.cert/
